<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:theme="@style/ThemeOverlay.LakuPandaiNTBSyariah.FullscreenContainer"
    tools:context=".ui.theme.CheckUpdateActivity">

    <ImageView
        android:id="@+id/logo_bank"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="48dp"
        android:gravity="center"
        android:keepScreenOn="true"
        android:src="@mipmap/logo_bank_ntb_syariah" />

    <ImageView
        android:id="@+id/logo_laku_pandai"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="-100dp"
        android:layout_marginHorizontal="100dp"
        android:gravity="center"
        android:keepScreenOn="true"
        android:src="@mipmap/logo_laku_pandai" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <LinearLayout
            android:id="@+id/fullscreen_content_controls"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="100dp"
            android:layout_gravity="bottom|center_horizontal"
            android:foregroundGravity="center"
            android:orientation="horizontal"
            tools:ignore="UselessParent">

            <ProgressBar
                android:id="@+id/progress_circular"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_marginEnd="8dp"
                android:layout_gravity="center"
                />

            <TextView
                android:id="@+id/version_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textStyle="bold"
                android:text="@string/process_check_update" />

        </LinearLayout>
    </FrameLayout>

</FrameLayout>